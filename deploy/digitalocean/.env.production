# ========================================
# DigitalOcean Production Environment
# ========================================
# This file contains production-specific overrides

# Domain configuration
DOMAIN_NAME=your-production-domain.com
SSL_EMAIL=admin@your-production-domain.com

# Database - Use DigitalOcean Managed Database in production
DATABASE_URL=postgresql://username:password@host:25060/database?sslmode=require

# App URLs
NEXT_PUBLIC_APP_URL=https://your-production-domain.com
BETTER_AUTH_URL=https://your-production-domain.com

# Production security keys (generate strong random keys)
BETTER_AUTH_SECRET=your-production-auth-secret-32-chars-minimum
ENCRYPTION_KEY=your-production-encryption-key-32-chars

# Container registry (update with your organization)
DOCKER_REGISTRY=ghcr.io
REPOSITORY_NAME=your-org/tel-agents-studio

# Production optimizations
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
VERCEL_TELEMETRY_DISABLED=1